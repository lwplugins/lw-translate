/vendor/
/dist/
/builds/
composer.lock
*.log
.phpcs.cache
.phpunit.cache/
.phpunit.result.cache

# Internal working notes (agent reports, review packages)
/.superpowers/

# React admin tooling (build/ is committed, never ignore it)
/node_modules/

# Local-only agent planning docs
/docs/superpowers/
